@model IEnumerable<QuanLySinhVien.Models.HocPhan>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admins/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenHocPhan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SoTinChi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenHocPhan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SoTinChi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaHocPhan }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaHocPhan }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaHocPhan })
        </td>
    </tr>
}

</table>
